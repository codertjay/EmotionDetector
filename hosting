Tutorial Url  https://www.digitalocean.com/community/tutorials/how-to-set-up-django-with-postgres-nginx-and-gunicorn-on-ubuntu-22-04


[Unit]
Description=gunicorn daemon
Requires=gunicorn.socket
After=network.target

[Service]
User=root
Group=www-data
WorkingDirectory=/root/EmotionDetector
ExecStart=/root/EmotionDetector/venv/bin/gunicorn \
        --access-logfile - \
        --workers 3 \
        --bind unix:/run/gunicorn.sock \
        PropertyManagement.wsgi:application

[Install]
WantedBy=multi-user.target


sudo nano /etc/nginx/sites-available/PropertyManagement


# Nginx Configuration
server {
    listen 80;
    server_name 139.144.54.183 ;

    location = /favicon.ico { access_log off; log_not_found off; }
    location /static/ {
        root /root/EmotionDetector;
    }
    location /media/ {
        root /root/EmotionDetector;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/run/gunicorn.sock;
    }
}


Celery Configuration systemd confuration in
sudo nano /etc/systemd/system/celery.service

[Unit]
Description=Celery Service
After=network.target

[Service]
User=root
Group=www-data
WorkingDirectory=/root/EmotionDetector
ExecStart=/root/EmotionDetector/venv/bin/celery -A PropertyManagement worker  --loglevel=info

[Install]
WantedBy=multi-user.target

sudo systemctl daemon-reload
sudo systemctl start celery
sudo systemctl enable celery
sudo systemctl status celery


sudo ln -s /etc/nginx/sites-available/EmotionDetector /etc/nginx/sites-enabled
